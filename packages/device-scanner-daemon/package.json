{
  "name": "@iml/device-scanner-daemon",
  "version": "0.0.0",
  "description": "A persistent process that consumes udev events over a unix domain socket.",
  "main": "dist/device-scanner-daemon.js",
  "scripts": {
    "prepublish": "old_pwd=$PWD; cd ../../; dotnet fable npm-run rollup --args \"-i $old_pwd/lib/server.fs -o $old_pwd/$npm_package_main\" --port 'free'"
  },
  "publishConfig": {
    "registry": "https://ubit-artifactory-or.intel.com/artifactory/api/npm/iml-npm-local"
  },
  "repository": "https://github.intel.com:IML/device-scanner/packages/device-scanner-daemon",
  "author": "IML Team",
  "license": "MIT"
}
